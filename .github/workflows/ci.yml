name: CI

on:
  push:
    branches: [master, main]
  pull_request:

jobs:
  check:
    name: Installer script checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Static + dry-run checks
        run: bash tests/check.sh
